let notas =[]; //declarado o vetor de notas 

function inserirNotas(){
    let nota = [];

    function inserirNotas() {
        let nota = Number(prompt("Digitar notas"));
        notas,Push
    }

    function Calcularmedia(){
        let.media = notas.reduce((media.nota)=> media+nota)/notas.length;
        console.log("A media é "+ media);
    }
    function menu(){
        let continuar = trua;
        while(continuar) {
            console.log("===Sistema de Notas===");
            console.log("|1 inerir Notas");
            console.log("|2 Calcular media");
            console.log("|3 Sair");
            switch (operador) {
                case "1"
                inserirNotas(;

                case "2"
                calcule a media;

                case "3"
                continuar = false;
                console.log ("informa uma opcao valida")

                
            }
        }
    }
